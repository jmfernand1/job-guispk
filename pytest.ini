[pytest]
# Los temporales de tmp_path/tmp_path_factory: de una corrida verde no queda
# nada, solo se conservan los de las que fallaron — que son los unicos que hay
# que mirar. Sin esto pytest guarda las tres ultimas corridas enteras, y los
# tests de respaldo escriben un .db por caso.
tmp_path_retention_policy = failed
